The purpose of this project is to use Docker and Spark with various data sources.

## 
Use MongoDB and MySQL as data sources, then use PySpark(local t^t) to transform the data. Finally, load the data into snowflake as a data warehouse.  
